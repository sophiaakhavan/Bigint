# Bigint
Class that defines a method to add two numbers of base 10 (decimal) by first converting from string to integer, performs the operation, then converts result back into a string to be outputted
